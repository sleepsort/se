支持拼写校正，利用k-gram进行修改
对wildcard query的支持还未写完，已完成一个经过测试的B-tree模板，
但因为目前只支持键值为int的情况，需要后续再改为支持string的代码

运行方式:
make clean # 清除运行文件
make reset # 清除现有索引
make 
make idx   # 重建索引
make sch < test/suggest # 对给定查询串校正
